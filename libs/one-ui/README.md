# ONE UI

## Running unit tests

Run `nx test one-ui` to execute the unit tests.

## Running storybook

npx nx storybook one-ui

## Create a one-ui component - reusable components

npx nx g component {component-name} --project=one-ui --export
